export interface StyledTheme {
    colors: {
        primary: string
        secondary: string,
        tertiary: string,
        gradient: string
    }

    text: {
        primaryFont: string,
        secondaryFont: string,
        color: string
    }

    brand: {
        icon: string
    }
    
    header: {
        borderRadius: string 
    }
}