export enum Category {
    FAVORITES = 'favorites',
    NOTIFICATIONS = 'notifications'
  }
  
  export enum GridType {
    SLOTS = 'slots',
    TOPBONUS = 'top bonus',
    BONUS = 'bonus',
    QUESTIONS = 'questions',
    FREE = 'free'
  }
  